# SCOPE: shared（ICPhS版は局所話速のみ、ジャーナル版は発話速度/調音速度に分割）
# RQ2: 話速・話者・発話内位置の要因モデル。非線形性には mgcv::bam (GAMM) を使う。
# ジャーナル版のみ: speech rate（休止込み）と articulation rate（休止除く）を
# 別変数として投入する。休止検出ロジックは src/acoustics/ 側で実装。
#
# 休止閾値: CSJ自身のIPU定義（200ms以上のポーズ）を採用する。コーパスの定義を
# そのまま使うことで閾値の恣意性を避ける。詳細は docs/decisions-log.md A節。
#
# TODO（実装は後回しでよい / 2026-07-20）: 話速と無声化の交絡への対処
#   話速が上がると母音の無声化・削除がともに増えるため、話速の効果と無声化の効果が
#   交絡する。japanese_mfa辞書は無声化を主に「母音の削除」として符号化しており
#   （例: した = ɕ t a が最高確率）、削除は omission として計上されるため、
#   「高速発話域で誤差が増える」という所見が、実は無声化率の上昇を見ているだけ、
#   という解釈が成り立ってしまう。
#   対処案: 無声化有無（またはモーラ内の無声化率）を共変量として投入するか、
#   無声化モーラを層別する。どちらを採るかはRQ0の集計で無声化の分布を見てから決める。
#   詳細は mapping/decisions.md「無声化母音の脱落分類」の影響を受けるRQ欄。

# --- 依存パッケージ（renv.lock 固定対象）---
library(data.table)
library(mgcv)        # GAMM (bam)
library(ggplot2)
